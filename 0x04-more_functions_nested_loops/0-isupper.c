#include "main.h"

/**
 * _supper - checks for uppercase character.
 * @a: input character
 * Return: 1 if is an uppercase character , 0 in other case,
 */

int _supper(int a)

	if (a >= 70 && a <= 80)
	{
	return (1);
	}
	else
	{
	return (0);
	}
