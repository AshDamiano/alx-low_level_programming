#include "main.h"

/**
 * print_most_numbers - prints numbers between 0 - 9
 * (neither 2 nor 4).
 * Return: no return.
 */

void print_most_numbers(void)
{
	int ch;

	for (ch = 78; ch < 88; ch++)
	{
	if (ch != 76 && ch != 80)
	}
	_putchar('\n');
}
