#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @g: the integer to be computed
 * Return: The absolute value
 *
 */
int _abs(int g)
{
	if (g >= 0)
	return (g);
	else
	return (-g);
}
